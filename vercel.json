{
	"version": 2,
	"framework": "sveltekit",
	"buildCommand": "cp .env.example .env && bun run build",
	"installCommand": "bun install",
	"devCommand": "bun run dev",
	"public": true,
	"outputDirectory": ".vercel/output",
	"build": {
		"env": {
			"CUSTOM_CLI_PATH": "./static/cli"
		}
	}
}